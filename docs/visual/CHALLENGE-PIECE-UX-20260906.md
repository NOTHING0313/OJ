# 棋子状态与手机操作验收（2026-09-06）

## 完成范围
- 棋盘角标：排队/判题中使用省略号，未通过/判题异常使用感叹号，已完成使用勾选标记。详情提供具体最近提交状态；完成状态优先，不因后续失败而降级。
- 状态查询严格限定当前账号及对应挑战任务，普通题目提交不混入；战队场景保留团队完成/得分与个人最近提交的区别。
- 手机宽度不超过 760px 或粗指针设备，点击棋子选中并滚动聚焦现有详情卡，再点击开始作答。手机详情优先显示、长描述内部滚动、按钮至少 44px。
- 题名、得分、状态及选中语义补齐；原红黄绿棋子种类文字保持。
- 未扩展挑战阶段或完成得分提示，不改数据库和计分。

## 验证
- 后端 TeamChallengeIntegrationTests：17 通过，包含最新提交顺序、其他账号隔离、普通提交排除、无记录回退及关联难度取值。
- npm run build 通过，保留原 Monaco 大包提示；相关页面/API/验收文件 ESLint、独立验收入口 TypeScript 通过。
- tests/challenge-flow.html：7/7 PASS。模拟接口和隔离存储，真实页面组件；新增手机两步点击、状态更新、历史完成不降级、难度红色检查，保留先前六组回归。
- 390×844 实际浏览器视口检查：点击第二颗棋子仍停留棋盘，选中详情获得焦点且开始作答按钮可见；已恢复默认视口。
- 按用户授权 npm ci 恢复锁定依赖；首次失败因本项目 esbuild 正在运行，停止本项目前端后恢复成功并重启。Worker 未重启；API 已切换到本轮 Verification 构建。
- 未执行真实答题写入、Docker 判题或多人压力测试。功能测试使用模拟页面和 EF InMemory，不能替代生产并发验证。
