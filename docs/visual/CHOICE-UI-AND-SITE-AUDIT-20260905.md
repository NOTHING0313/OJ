# 选择题修复与站点视觉检查 — 2026-09-05

## 已完成

- 修正选择题选项被通用表单样式覆盖的问题：选择框、字母和 Markdown 首行对齐；长内容在内容列内换行。
- 移除选择题查看/作答页的“编辑题目”，保留题目管理列表的编辑入口。
- 新建完整选择题可勾选发布并点击“创建并发布”。后端复用完整性校验和不可变版本发布器，通过关系数据库事务保存。
- 修正手机端共享导航文字被挤成单字竖排的问题。
- 无 DTO 或数据库结构变更；没有推送、部署或安装依赖。

## 实际交互证据

环境：本地 Vite `localhost:5173`、API `localhost:5101`、PostgreSQL；Root 登录。桌面 1366×900，手机视口 390×844。使用浏览器渲染截图与 DOM 布局检查，不以构建成功代替视觉验收。

- 新建 `[视觉验收]直接发布选择题`，填写完整单选题、Markdown 选项及长选项，勾选发布后直接创建成功。
- 编辑页保留已发布状态，题目管理列表显示已发布。
- 查看页没有编辑入口；提交正确选项后结果为 1/1。
- 原 `[E2E]选择题测试` 的五个多选项逐项检查：字母与内容顶部坐标一致，选择框顶部偏移 4 px 以对齐文字；实际截图恢复同行排版。
- 测试题 ID：`783ef632-6aea-4c67-beb1-70e256140e38`。
- 测试提交 ID：`c74d3571-a8c5-49d9-a662-6737bb3383bc`。
- 上述本地测试数据保留供复查；未删除或修改原有测试题。

## 视觉覆盖

以下为页面呈现检查；除上面的选择题流程外，不代表每个按钮或全部业务状态均已功能回归。

| 模块 | 已检查页面/状态 | 限制 |
|---|---|---|
| 题目 | 列表；原多选题；新单选题；编程题；新建/编辑；编程测试点管理 | 未遍历所有语言编辑器及全部 Markdown 组合 |
| 提交 | 我的提交、管理员提交列表、选择题结果 | 未重新执行所有代码判题状态 |
| 用户 | 个人中心、账户设置、管理员用户列表 | 当前只有 Root 账号；普通答题者交互未覆盖 |
| 挑战 | 挑战列表、管理列表、新建表单 | 无挑战数据，详情、任务、提交及评审状态未覆盖 |
| 战队 | Root 战队入口、管理列表 | 无战队数据，聊天、项目及历史详情未覆盖 |
| 榜单 | 榜单入口、赛季管理空态 | 无赛季数据，实际排名与归档详情未覆盖 |
| 帮助 | 帮助首页、管理列表、新建表单 | 无帮助内容，详情未覆盖 |
| 站点管理 | 安全审计列表、主题编辑器 | 未修改主题；未遍历每个主题控件 |
| 登录 | 登录页和 Root 登录后状态 | 未重新操作注册及找回密码流程 |

手机端额外检查：题目列表、管理列表、选择题查看/编辑/提交结果、编程题、账户设置及主题编辑器。已检查页面没有文档级横向溢出；宽表格仍需容器内横向浏览。

## 视觉/体验发现（含后续修复状态）

1. 已修复：手机选择题编辑器嵌套卡片与边距过多，选项输入区域偏窄，上传图片等按钮会换行。后续局部修复减少小屏嵌套留白、分行排列题号与操作按钮，并保持上传按钮文字完整。
2. 当时 Root 直接访问 `/account/competition` 显示原始英文 `Forbidden`。后续按用户要求挂起整个页面：隐藏“我的赛季战绩”入口，旧地址跳转到个人中心。更正：页面实际名称是“赛季战绩”，此前称“参赛资料”不准确。实现与后端数据保留，等待明确需求再恢复；见 DecisionLog 的挂起记录。
3. 已修复：选择题提交详情按题型隐藏语言与运行时间/内存字段；个人和管理员共用提交列表的选择题行合并资源指标单元格显示得分，编程提交保持原指标。

### 后续局部修复验收（同日）

- 范围为用户批准的第 1、2 项；英文 Forbidden 问题保持待处理。风险初判与终判均为 R1，无 API、持久化或权限变更。
- 本轮源码仅修改 `frontend/src/styles.css`、`frontend/src/pages/SubmissionDetailPage.tsx`、`frontend/src/pages/MySubmissionsPage.tsx`；使用原有题型字段与组件，未新增依赖。
- UI 验证：360/390/768 px 编辑页及 1366 px 桌面编辑页；390 px 选择题详情；1366 px 编程详情、个人/管理员混合提交列表。360/390 px 下选项输入框分别为 256/286 px，文档无横向溢出；上传按钮文字完整，平板题号与操作区分行。未保存编辑或新增验收数据。
- `npm run typecheck`、两个改动 TSX 的定向 ESLint 和 `git diff --check` 均通过（退出码 0）。采用 UI 验证范围；本轮不重复全量构建、后端测试或增加镜像实现的单元测试，因为仅有 CSS 与渲染分支变更。
- 得分使用直接数值插值，未按真值过滤；零分不会被隐藏。此次已有浏览器样本为 1/1，未新增零分提交。

## 验证结果

- Release 下 `ChoiceProblemTests|ProblemJudgeRevisionTests`：15 通过，1 跳过；跳过项需要未配置的 PostgreSQL 并发集成测试环境。实际浏览器创建流程使用本地 PostgreSQL，但不替代并发测试。
- `ProblemEditorSourceTests`：5 通过。
- 前端 choiceOptions/problemDrafts/submissionPolling：13 通过。
- 改动的三个 TSX 文件定向 ESLint：通过。
- 最终 `npm run build`：类型检查、Vite 构建及包体预算通过；仍有 Monaco 相关大 chunk 提示。
- 首次 Debug 后端测试被运行中的 API/Worker DLL 文件锁阻止，随后使用 Release 完成上述检查；未将首次失败当作通过。
- 本地 API 已切换到本次 Release 构建以进行浏览器验收；Worker 未重启。

结论：用户指定的三个问题已修复并完成真实流程验证。已完成当前账号和数据可访问模块的视觉巡检；缺失数据、角色及动态状态仍属未验证，不能宣称全站所有场景通过。

页面挂起验证：`npm run typecheck`、main.tsx 与 LeaderboardHomePage.tsx 定向 ESLint 通过；浏览器原 `/account/competition` 标签已跳到 `/profile/me` 并正常呈现个人中心。本次仅前端入口与路由调整，无后端或全量测试重跑。
