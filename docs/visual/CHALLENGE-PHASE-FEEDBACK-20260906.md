# 挑战阶段与完成反馈（2026-09-06）

## 范围
前端局部行为修复（R1）：ChallengeDetailPage、styles.css、challenge-flow 浏览器回归；无接口、数据库或计分变更。

## 实现
- 按后台 IsChallengeOpen 的闭区间规则显示未开始/进行中/已结束，每秒更新并在窗口恢复焦点时更新；作答点击即时重新核对时间。未开始和结束仍可选中查看棋子说明，但作答按钮禁用，桌面点击棋子也不会直接进入作答。
- 阶段提示为浏览器时间下的交互引导，服务器仍对每次提交做最终校验；设备时钟偏差或已进入作答页后跨截止时刻，均由既有服务端规则拒绝越期提交。
- 成功返回棋盘时显示六秒提示，读取挑战详情中已确认的得分及当前完成数，不宣称新增分数；数据未同步时明确显示正在同步。文件题只提示上传完成、等待评分。
- 提示支持手动关闭，沿用现有动画 nonce 去重；轮询不重复弹出。减少动态效果设置仍沿用已有动画处理。

## 验证
- 应用 TypeScript、定向 ESLint、验收入口 TypeScript 检查通过；差异空白检查通过。
- 浏览器真实组件/模拟接口回归覆盖时间自动切换、开始/截止边界、结束后禁用、得分及进度提示、自动关闭与手动关闭、文件题待评分；并保留此前交互、状态、恢复和竞态回归。
- 未修改实际挑战、提交真实答案或运行完整后端测试；阶段规则已对照后台实现。
- 手机视觉校验发现父级入场动画使 fixed 提示偏离视口，改为 React portal 挂载到 document.body 后，390px 视口下完整显示提示及关闭按钮，已恢复默认视口。
- 最终浏览器 9/9 PASS。桌面点击用例显式固定非触控输入条件，手机用例独立模拟触控；修正了手机视口残留导致原桌面用例前提不成立的问题，未改变生产行为。
