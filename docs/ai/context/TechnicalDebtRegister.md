# Technical Debt Register

| Debt ID | Area | File / concept | Reason accepted | Risk | Repayment trigger | Next action | Status |
|---|---|---|---|---|---|---|---|
| JUDGE-REV-FND-D001 | Judge asset retention | `ProblemJudgeAssetService` and judge-asset storage | Historical revisions must keep immutable asset content; safe reachability analysis is outside Stage 1 | Repeated asset replacement can increase disk use | Before production revision rollout and storage-capacity alerting | Add audited, idempotent garbage collection for soft-deleted assets with no revision references | Open |
| JUDGE-REV-BIND-D001 | Queue delivery and recovery | `SubmissionService`, `RedisJudgeQueue`, and `JudgeWorker` | Stage 2 preserved the existing submission-ID Redis contract pending the approved reliability stage | Historical risk: Redis could expose work before commit or lose popped work after failure | Repaid by `JUDGE-QUEUE-RELIABILITY-03` | PostgreSQL JudgeJob authority, fenced leases, bounded retry/dead-letter, and Redis-independent polling implemented and verified | Resolved 2026-09-03 |
| PLATFORM-04A-D001 | Frontend lint depth | React Hooks dependencies and Fast Refresh export boundaries | Deferred from 4A until route/module ownership could be corrected coherently | Historical risk: stale closures and invalid refresh modules were not blocking | Repaid by Stage 4E | Stable callbacks, dedicated helper modules and explicit hook-export allowances implemented; both rules now block lint | Resolved 2026-09-03 |
| PLATFORM-04A-D002 | Hosted Docker CI | GitHub Actions judge and sandbox security gates | No repository-owned Docker-capable runner contract is confirmed; adding an assumed self-hosted label would not create an executable gate | Docker regressions are detected locally but not automatically on pull requests | Before production release automation is treated as unattended | Provision/identify a Linux Docker runner and invoke the existing judge smoke plus security smoke on manual/nightly runs | Open |
| SANDBOX-04B-D001 | Sandbox disk quota | Docker judge workspace write limits | Existing security smoke proved cleanup but explicitly reported `FILE_SPAM_DISK_QUOTA=REMAINING_RISK` | Historical risk: runtime submissions could consume host workspace disk before cleanup | Repaid by `SANDBOX-DATAPLANE-04B` | Runtime bind mounts are read-only; `/tmp`, file size, and combined output are bounded; the probe is blocking | Resolved 2026-09-03 |
| SANDBOX-04B-D002 | Compile workspace aggregate quota | Writable compiler bind mount | Supported compilers need to emit fixed artifacts into the host workspace; Docker bind mounts have no portable aggregate size option | A pathological compiler workload can create several individually capped files before the 30-second compile timeout and cleanup | Before allowing custom build scripts, compiler plugins, or additional language toolchains | Move compilation to a quota-backed ephemeral workspace and copy out an allowlist of bounded runtime artifacts | Open |
| AUTH-04C-D001 | Browser-session deployment | HTTPS termination and forwarded scheme | Secure host-only cookies and antiforgery intentionally fail on plain HTTP; versioned Nginx assets now preserve the forwarded scheme but the real origin is unverified | Browser login cannot operate if the target host is exposed without valid TLS and proxy headers | Before any production browser-session rollout | Issue the real certificate, run `nginx -t`, verify cookie/CSRF through the public origin, and record renewal evidence | Open; local configuration verified |
| AUTH-04C-D002 | Dormant phone compatibility | Backend SMS routes, DTO fields and database columns | The product no longer presents phone binding/recovery, but breaking API/schema removal was not authorized and would add migration risk | Dormant surface may confuse future maintainers; calls fail closed in Production without a real sender | When the project explicitly approves permanent phone-feature removal or reintroduction | Inventory external callers, then either remove routes/schema through a migration or add a supported production sender and UI | Open |
| PROD-04D-D001 | Target-host deployment evidence | `deploy/production` topology and operations scripts | Local assets and isolated containers are verified, but no remote write, real certificate, SMTP delivery, real-data restore or target resource run was authorized | Production readiness cannot be claimed from local syntax/runtime checks alone | Before public launch | Execute the documented target-host TLS/systemd/SMTP/backup-restore/sandbox/resource matrix and retain evidence | Open |
| FRONTEND-04E-D001 | Optional editor payload | Monaco chunk loaded by `ProblemDetailPage` | Route splitting removes Monaco from initial/login/problem-list loads; replacing or deeply restructuring the required editor is outside the stable-PC scope | First visit to a code-editing problem downloads an approximately 2.69 MiB minified chunk | If measured dorm-network editor startup is unacceptable or mobile/weak-network support returns | Measure on the target host, then evaluate Monaco feature pruning or an alternate editor without weakening judge UX | Open / accepted for current environment |
| EXPORT-04C-D001 | Spreadsheet compatibility verification | Challenge CSV downloads | Code-level malicious-cell tests passed, but native Office automation is unavailable and LibreOffice installation/download was blocked by host policy | A spreadsheet-specific parsing difference could remain undetected | When Excel/LibreOffice is available or in release-validation CI | Import generated adversarial CSVs in Excel and LibreOffice and confirm text/numeric/date interpretation | Open |
